# 🚀 vnesenie-v-reestr.ru - Проект с автоматическим деплоем

## 📋 Описание
Сайт для внесения в реестры и сертификации с функциональностью сохранения leads в базу данных.

## 🔧 Технологии
- PHP 7.4+
- MySQL/MariaDB
- HTML/CSS/JavaScript
- Git для версионирования

## 🚀 Быстрый старт

### 1. **Локальная разработка:**
```bash
# Клонирование репозитория
git clone <your-repo-url>
cd vnesenie-v-reestr.ru

# Создание новой ветки для разработки
git checkout -b feature/new-feature

# Внесение изменений и коммит
git add .
git commit -m "Описание изменений"

# Отправка изменений
git push origin feature/new-feature
```

### 2. **Деплой на сервер:**
```bash
# Переключение на main ветку
git checkout main

# Слияние изменений
git merge feature/new-feature

# Деплой на сервер
./deploy_to_server.sh
```

## 📁 Структура проекта
```
vnesenie-v-reestr.ru/
├── admin/           # Административная панель
├── config/          # Конфигурационные файлы
├── models/          # Модели данных
├── news/            # Новости
├── telegram_bot/    # Telegram бот
├── uploads/         # Загруженные файлы (исключены из Git)
├── deploy_to_server.sh  # Скрипт деплоя
└── README.md        # Эта инструкция
```

## 🔄 Рабочий процесс

### **Ежедневная работа:**
1. **Создайте ветку** для новой функции
2. **Разрабатывайте** в локальной среде
3. **Тестируйте** изменения
4. **Коммитьте** и пушите в feature ветку
5. **Сливайте** в main когда готово
6. **Деплойте** на сервер через `./deploy_to_server.sh`

### **Деплой:**
```bash
# Простой деплой
./deploy_to_server.sh

# Или пошагово:
rsync -avz -e "ssh -p 8228" --exclude=.git --exclude=uploads ./ s261262@ruvip55.hostiman.ru:/var/www/s261262/data/www/vnesenie-v-reestr.ru/
```

## 🗄️ База данных
- **Хост:** ruvip55.hostiman.ru:3306
- **База:** leads
- **Пользователь:** haibaneden
- **Пароль:** Stilesmile1

## 📝 Функциональность leads
Все формы сайта автоматически сохраняют данные в таблицу `leads`:
- Имя, телефон, email
- Сообщение/комментарий
- Услуга/страница
- IP адрес, User Agent
- UTM метки
- Временная метка

## 🚨 Важные замечания
- Папка `uploads/` исключена из Git (слишком большая)
- Большие файлы (.zip, .xlsx) не коммитятся
- Конфигурационные файлы с паролями исключены
- Telegram бот логи исключены

## 🆘 Поддержка
При возникновении проблем:
1. Проверьте права доступа на сервере
2. Убедитесь, что SSH ключи настроены
3. Проверьте PHP синтаксис: `php -l filename.php`
4. Проверьте логи сервера

---
**🎯 Проект готов к работе! Используйте `./deploy_to_server.sh` для быстрого деплоя.**
